# No credential belongs in VITE_*: these variables are bundled into the frontend.
# Enter the Riot development key in Settings; it is kept in memory for the session.
# No environment file is required to run the demonstration.
